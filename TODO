$Id$

- Core Features

  x Unsubsribe Members / Anonymous
  o Anonymous subscriptions management
  x ComputedRecipientsRule
  o WorkflowImpliedRecipientsRule to implement
 
- Notifications

  x Better emails generation (Encoding, stuffs...)
  o Full i18n for emails based on Mailing list language ? 

- Subscriptions Tool 

  o Export configuration
  o Context -> event -> content instead of event -> content
  o Ability to specify events for which we'll make a render of the object 
    instead of using 
- Default 

  o What events ? 
  o Where ? 

- Misc 

  o Usuability
  o Local for subscriptions.
